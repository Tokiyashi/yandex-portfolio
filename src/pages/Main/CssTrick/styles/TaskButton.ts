import {Button, styled} from "@mui/material";

const TaskButton = styled(Button)`
  width: 20rem;
  box-shadow: 1rem 0.6rem 0 black;
  border-radius: 0.5rem;
  height: 4rem;
`;
export default TaskButton;